sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t wemistic22/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true wemistic22/react-test npm test

after_success:
  - docker build -t wemistic22/multi-client ./client
  - docker build -t wemistic22/multi-nginx ./nginx
  - docker build -t wemistic22/multi-server ./server
  - docker build -t wemistic22/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push 968340/multi-docker/multi-client
  - docker push 968340/multi-docker/multi-nginx
  - docker push 968340/multi-docker/multi-server
  - docker push 968340/multi-docker/multi-worker
