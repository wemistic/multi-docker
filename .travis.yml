sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t wemistic/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true wemistic/react-test npm test

after_success:
  - docker build -t wemistic/multi-client ./client
  - docker build -t wemistic/multi-nginx ./nginx
  - docker build -t wemistic/multi-server ./server
  - docker build -t wemistic/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push wemistic/multi-client
  - docker push wemistic/multi-nginx
  - docker push wemistic/multi-server
  - docker push wemistic/multi-worker
